---
phase: 02-shared-logic-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/meditation-content/package.json
  - packages/meditation-content/tsconfig.json
  - packages/meditation-content/vitest.config.ts
  - packages/meditation-content/src/index.ts
  - packages/meditation-content/src/quotes.ts
  - packages/meditation-content/src/__tests__/quotes.test.ts
  - packages/meditation-core/src/index.ts
  - packages/meditation-core/src/types.ts
  - packages/meditation-core/src/storage.ts
  - packages/meditation-core/src/errors.ts
  - packages/meditation-core/src/__tests__/types.test.ts
  - packages/meditation-core/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [MONO-06, MONO-08, MONO-10]

must_haves:
  truths:
    - "meditation-content package exists with quotes data"
    - "meditation-core exports TypeScript types used by web app"
    - "Storage adapter interface defined matching Zustand StateStorage"
    - "Storage interface is platform-agnostic (works for web localStorage and mobile MMKV)"
    - "Custom error classes exported for apps to use"
  artifacts:
    - path: "packages/meditation-content/src/quotes.ts"
      provides: "Stoic quotes array and getRandomQuote function"
      exports: ["stoicQuotes", "getRandomQuote", "Quote"]
    - path: "packages/meditation-core/src/types.ts"
      provides: "Mood and entry type definitions"
      exports: ["PreMood", "PostMood", "MoodEntry", "SoundType"]
    - path: "packages/meditation-core/src/storage.ts"
      provides: "StateStorage interface matching Zustand"
      contains: "getItem, setItem, removeItem"
      min_lines: 10
    - path: "packages/meditation-core/src/errors.ts"
      provides: "Custom error classes"
      exports: ["StorageError", "SerializationError", "StoreInitError"]
  key_links:
    - from: "packages/meditation-core/src/index.ts"
      to: "types.ts, storage.ts, errors.ts"
      via: "re-exports all public types"
      pattern: "export.*from.*types|storage|errors"
    - from: "packages/meditation-core/src/storage.ts"
      to: "zustand/middleware StateStorage interface"
      via: "compatible signature for cross-platform use"
      pattern: "getItem.*string.*null|setItem.*string.*void"
---

<objective>
Create meditation-content package for quotes and establish meditation-core type foundations (types, storage interface, errors) that both web and mobile apps will use.

Purpose: Separate content data from business logic, define shared contracts for storage abstraction, enable type-safe imports across platforms. Prepare StateStorage interface for Phase 3 mobile MMKV implementation.

Output: Two workspace packages with passing tests, ready for store extraction in Wave 2.
</objective>

<execution_context>
@/Users/hyojin.yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojin.yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-logic-extraction/02-CONTEXT.md
@.planning/phases/02-shared-logic-extraction/02-RESEARCH.md
@.planning/phases/01-monorepo-foundation/01-01-SUMMARY.md
@.planning/phases/01-monorepo-foundation/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Existing code patterns from web app that will be extracted -->

From apps/web/src/lib/meditation-store.ts (types to extract):
```typescript
export type PreMood = 'stressed' | 'tired' | 'neutral' | 'anxious';
export type PostMood = 'calm' | 'relieved' | 'peaceful' | 'grateful' | 'refreshed';
export type SoundType = 'singing-bowl' | 'gong' | 'ambient-pad' | 'nature' | 'rain' | 'ocean' | 'wind' | 'birds' | 'fireplace' | 'random';

export interface MoodEntry {
  id: string;
  preMood: PreMood;
  postMood?: PostMood;
  timestamp: number;
  note?: string;
  sessionMinutes?: number;
  sound?: SoundType;
  savedQuote?: { text: string; author: string };
}
```

From apps/web/src/lib/meditation-store.ts (quotes to extract):
```typescript
export const stoicQuotes = [
  { text: "The happiness of your life depends upon the quality of your thoughts.", author: "Marcus Aurelius" },
  // ... 14 more quotes
];

export const getRandomQuote = () => stoicQuotes[Math.floor(Math.random() * stoicQuotes.length)];
```

Zustand StateStorage interface (target for storage.ts):
```typescript
interface StateStorage {
  getItem: (name: string) => string | null | Promise<string | null>;
  setItem: (name: string, value: string) => void | Promise<void>;
  removeItem: (name: string) => void | Promise<void>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create meditation-content package with quotes</name>
  <files>
    packages/meditation-content/package.json
    packages/meditation-content/tsconfig.json
    packages/meditation-content/vitest.config.ts
    packages/meditation-content/src/index.ts
    packages/meditation-content/src/quotes.ts
    packages/meditation-content/src/__tests__/quotes.test.ts
  </files>
  <behavior>
    - stoicQuotes array has 15 Quote objects with text and author fields
    - getRandomQuote returns a Quote object from stoicQuotes
    - getRandomQuote returns different quotes on multiple calls (randomness works)
    - Quote type enforces text and author as required strings
  </behavior>
  <action>
    Create meditation-content package scaffold following meditation-core structure from Phase 1.

    1. Create packages/meditation-content/ directory
    2. Create package.json:
       - name: "@repo/meditation-content"
       - version: "0.0.0"
       - private: true
       - type: "module"
       - exports field with types first: `{ ".": { "types": "./src/index.ts", "default": "./src/index.ts" } }`
       - devDependencies: @repo/typescript-config (workspace:*), typescript, vitest, @types/node
       - scripts: test (vitest run), test:watch (vitest)
    3. Create tsconfig.json extending @repo/typescript-config/base.json with include: ["src"]
    4. Create vitest.config.ts with node environment, no setupFiles
    5. Write tests in src/__tests__/quotes.test.ts FIRST:
       - Test: stoicQuotes has 15 items
       - Test: getRandomQuote returns valid Quote object with text and author
       - Test: getRandomQuote returns different quotes (run 20 times, check uniqueness)
    6. Create src/quotes.ts:
       - Define Quote interface: `{ text: string; author: string }`
       - Export stoicQuotes array (copy all 15 quotes from apps/web/src/lib/meditation-store.ts lines 81-96)
       - Export getRandomQuote function
    7. Create src/index.ts re-exporting: `export { stoicQuotes, getRandomQuote, type Quote } from './quotes';`
    8. Run `pnpm --filter @repo/meditation-content install` to link dependencies
    9. Run tests until all pass

    Follow Phase 1 JIT package pattern (no build step). Match meditation-core's structure exactly.
  </action>
  <verify>
    <automated>pnpm --filter @repo/meditation-content test</automated>
  </verify>
  <done>
    meditation-content package exists with 3/3 tests passing, exports Quote type, stoicQuotes array, and getRandomQuote function
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract types and storage interface to meditation-core</name>
  <files>
    packages/meditation-core/src/types.ts
    packages/meditation-core/src/storage.ts
    packages/meditation-core/src/errors.ts
    packages/meditation-core/src/index.ts
    packages/meditation-core/src/__tests__/types.test.ts
    packages/meditation-core/package.json
  </files>
  <behavior>
    - PreMood type accepts only 'stressed', 'tired', 'neutral', 'anxious'
    - PostMood type accepts only 'calm', 'relieved', 'peaceful', 'grateful', 'refreshed'
    - SoundType includes all 10 sound options
    - MoodEntry type matches existing structure with optional fields
    - preMoodLabels and postMoodLabels provide human-readable strings
    - preMoodColors and postMoodColors provide Tailwind class strings
    - StateStorage interface matches Zustand's built-in interface
    - StorageError, SerializationError, StoreInitError extend Error correctly
    - instanceof checks work for custom error classes
  </behavior>
  <action>
    Extract core types from meditation-store.ts to meditation-core, keeping UI dependencies in web app.

    1. Write tests in src/__tests__/types.test.ts FIRST:
       - Test: PreMood type compiles with valid values (TypeScript test)
       - Test: MoodEntry type compiles with required and optional fields
       - Test: preMoodLabels has all 4 pre-moods with string labels
       - Test: preMoodColors has all 4 pre-moods with Tailwind classes
       - Test: postMoodLabels has all 5 post-moods
       - Test: postMoodColors has all 5 post-moods
    2. Create src/types.ts:
       - Export type definitions: PreMood, PostMood, SoundType (copy from meditation-store.ts lines 8-11)
       - Export MoodEntry interface (copy from lines 13-22)
       - Export preMoodLabels: `Record<PreMood, string>` with labels only (no icons)
       - Export postMoodLabels: `Record<PostMood, string>` with labels only
       - Export preMoodColors: `Record<PreMood, string>` with Tailwind classes from existing config
       - Export postMoodColors: `Record<PostMood, string>` with Tailwind classes
       - DO NOT import Lucide icons (UI dependency stays in web app)
    3. Create src/storage.ts:
       - Export StateStorage interface matching Zustand: `{ getItem, setItem, removeItem }`
       - Add JSDoc comment: "@interface StateStorage - Platform-agnostic storage adapter interface compatible with Zustand persist middleware. Web apps use localStorage, mobile apps use MMKV."
       - Add JSDoc for each method explaining synchronous/async support
       - Export createStorageAdapter helper if needed for validation
    4. Create src/errors.ts:
       - Create base MeditationError class extending Error
       - Use Object.setPrototypeOf for proper instanceof behavior
       - Create StorageError, SerializationError, StoreInitError extending MeditationError
       - Export type guard helpers: isStorageError, isSerializationError, isStoreInitError
    5. Update src/index.ts to re-export all from types, storage, errors
    6. Add date-fns to package.json dependencies (needed for utilities in next plan)
    7. Run `pnpm --filter @repo/meditation-core install`
    8. Run tests until all pass

    Keep placeholder exports (PACKAGE_NAME, validatePackage) alongside new exports for now.

    CRITICAL: StateStorage interface must be platform-agnostic to satisfy MONO-10 (mobile storage adapter interface prepared). The interface signature works for both web localStorage (synchronous) and mobile MMKV (can be async).
  </action>
  <verify>
    <automated>pnpm --filter @repo/meditation-core test</automated>
  </verify>
  <done>
    meditation-core exports all types, storage interface, and error classes with passing tests. TypeScript compilation succeeds. preMoodLabels/Colors and postMoodLabels/Colors exported without icon dependencies. StateStorage interface documented as platform-agnostic and ready for mobile MMKV adapter in Phase 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update workspace dependencies</name>
  <files>pnpm-lock.yaml</files>
  <action>
    Run pnpm install at root to update lockfile with new packages and dependencies.

    ```bash
    pnpm install
    ```

    Verify workspace packages discovered:
    - @repo/meditation-core
    - @repo/meditation-content
    - @repo/typescript-config
    - simple-meditation-monorepo (root)

    Check that meditation-core's date-fns dependency resolved correctly.
  </action>
  <verify>
    <automated>pnpm list --recursive | grep -E "@repo/meditation-(core|content)"</automated>
  </verify>
  <done>
    pnpm-lock.yaml updated, both packages discovered by workspace, all dependencies resolved
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**

1. meditation-content tests pass (3/3 tests green)
2. meditation-core tests pass (existing + new type tests green)
3. pnpm workspace discovery shows 4 packages
4. TypeScript compilation succeeds for both packages
5. Exports fields configured correctly (types first)
6. No Lucide icon imports in meditation-core or meditation-content
7. StateStorage interface documented as platform-agnostic for mobile use
</verification>

<success_criteria>
- [ ] meditation-content package created with quotes and getRandomQuote
- [ ] meditation-core exports PreMood, PostMood, MoodEntry, SoundType types
- [ ] meditation-core exports StateStorage interface matching Zustand
- [ ] StateStorage interface documented for cross-platform use (MONO-10)
- [ ] meditation-core exports custom error classes with type guards
- [ ] meditation-core exports mood labels and colors without icon dependencies
- [ ] All tests passing in both packages
- [ ] pnpm workspace recognizes both packages
- [ ] No UI dependencies (Lucide icons) in shared packages
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-logic-extraction/02-01-SUMMARY.md`
</output>
