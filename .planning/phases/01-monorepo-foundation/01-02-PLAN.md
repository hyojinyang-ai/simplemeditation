---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/tsconfig.node.json
  - apps/web/vitest.config.ts
  - apps/web/src/**/*
  - apps/web/public/**/*
  - apps/web/index.html
autonomous: true
requirements: [MONO-03]

must_haves:
  truths:
    - "Web app builds successfully in new apps/web/ location"
    - "Dev server runs on localhost:8080 as before"
    - "All existing tests pass without modification"
    - "Path alias @/ resolves correctly to src/"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Web app package configuration"
      contains: "@repo/web"
    - path: "apps/web/vite.config.ts"
      provides: "Vite build configuration"
      contains: "port: 8080"
    - path: "apps/web/src/main.tsx"
      provides: "React application entry point"
      min_lines: 5
  key_links:
    - from: "apps/web/package.json"
      to: "@repo/typescript-config"
      via: "workspace:* dependency"
      pattern: "\"@repo/typescript-config\": \"workspace:\\*\""
    - from: "apps/web/tsconfig.json"
      to: "packages/typescript-config/react.json"
      via: "extends field"
      pattern: "\"extends\": \"@repo/typescript-config/react.json\""
    - from: "apps/web/vite.config.ts"
      to: "apps/web/src"
      via: "path alias"
      pattern: "@.*resolve.*src"
---

<objective>
Migrate existing web application to apps/web/ directory within monorepo structure, preserving all functionality.

Purpose: Move the current single-app codebase into the monorepo apps/ directory, updating configurations to work in the new location while ensuring the web app continues to build and run identically to before.

Output: Web app successfully relocated to apps/web/ with all features working, development server on port 8080, existing tests passing, ready to import shared packages in Phase 2.
</objective>

<execution_context>
@/Users/hyojin.yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojin.yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Interface context for web app structure
<interfaces>
<!-- Key files that must be moved -->

Current structure (root):
- src/ (entire directory with components, pages, lib, hooks, integrations, test)
- public/ (sounds, images, videos)
- index.html
- package.json
- vite.config.ts
- tsconfig.json, tsconfig.app.json, tsconfig.node.json
- vitest.config.ts
- tailwind.config.ts
- postcss.config.js
- eslint.config.js
- components.json

Target structure (apps/web/):
- All of the above moved into apps/web/ subdirectory
- Path aliases remain relative to apps/web/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apps/web/ and move source files</name>
  <files>apps/web/src/, apps/web/public/, apps/web/index.html</files>
  <action>
    Create apps/web/ directory and move the following from root to apps/web/:
    - src/ (entire directory with all subdirectories)
    - public/ (sounds, images, videos)
    - index.html

    Use `git mv` for all moves to preserve history. Do NOT move:
    - .planning/ (stays at root)
    - .git/ (stays at root)
    - node_modules/ (will be deleted and regenerated)
    - CLAUDE.md, README.md, ANALYTICS.md (documentation stays at root)

    After moving, verify that key files exist in new locations:
    - apps/web/src/main.tsx
    - apps/web/src/App.tsx
    - apps/web/public/sounds/singing-bowl.mp3
    - apps/web/index.html
  </action>
  <verify>
    <automated>test -f apps/web/src/main.tsx && test -f apps/web/src/App.tsx && test -f apps/web/index.html && test -d apps/web/public/sounds</automated>
  </verify>
  <done>Source files moved to apps/web/ with git history preserved, directory structure intact</done>
</task>

<task type="auto">
  <name>Task 2: Move and update configuration files</name>
  <files>apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/tsconfig.app.json, apps/web/tsconfig.node.json, apps/web/vitest.config.ts, apps/web/tailwind.config.ts, apps/web/postcss.config.js, apps/web/eslint.config.js, apps/web/components.json</files>
  <action>
    Move configuration files from root to apps/web/ using `git mv`:
    - vite.config.ts
    - tsconfig.json, tsconfig.app.json, tsconfig.node.json
    - vitest.config.ts
    - tailwind.config.ts
    - postcss.config.js
    - eslint.config.js
    - components.json

    Update vite.config.ts path alias to be relative to apps/web/:
    ```typescript
    resolve: {
      alias: {
        "@": path.resolve(__dirname, "./src"),  // Now relative to apps/web/
      },
    }
    ```

    Preserve existing port 8080 configuration - do NOT change it.

    Update tsconfig.json to extend shared config:
    ```json
    {
      "extends": "@repo/typescript-config/react.json",
      "compilerOptions": {
        "outDir": "dist",
        "paths": {
          "@/*": ["./src/*"]
        }
      },
      "include": ["src"]
    }
    ```

    Keep tsconfig.app.json and tsconfig.node.json as-is (they reference tsconfig.json which now extends shared config).

    vitest.config.ts needs no changes as it uses relative paths.
  </action>
  <verify>
    <automated>test -f apps/web/vite.config.ts && test -f apps/web/tsconfig.json && grep -q "@repo/typescript-config/react.json" apps/web/tsconfig.json && grep -q "port: 8080" apps/web/vite.config.ts</automated>
  </verify>
  <done>Configuration files moved and updated with correct paths, tsconfig extends shared config, port 8080 preserved</done>
</task>

<task type="auto">
  <name>Task 3: Create apps/web/package.json</name>
  <files>apps/web/package.json</files>
  <action>
    Copy root package.json to apps/web/package.json and update:

    1. Change name to "@repo/web"
    2. Keep all existing dependencies (copy from root package.json)
    3. Add devDependency: "@repo/typescript-config": "workspace:*"
    4. Keep all existing scripts unchanged:
       - "dev": "vite"
       - "build": "vite build"
       - "build:dev": "vite build --mode development"
       - "lint": "eslint ."
       - "preview": "vite preview"
       - "test": "vitest run"
       - "test:watch": "vitest"
    5. Preserve "type": "module" and "private": true

    After creating apps/web/package.json, remove the old root package.json dependencies section (keeping only the monorepo fields: name, private, packageManager, scripts with turbo commands, devDependencies with just turbo).

    The web app will now be an independent workspace package that pnpm links.
  </action>
  <verify>
    <automated>test -f apps/web/package.json && grep -q '"@repo/web"' apps/web/package.json && grep -q '"@repo/typescript-config": "workspace:\*"' apps/web/package.json</automated>
  </verify>
  <done>apps/web/package.json created with correct name, dependencies preserved, typescript-config workspace dependency added</done>
</task>

<task type="auto">
  <name>Task 4: Reinstall dependencies and verify build</name>
  <files>pnpm-lock.yaml</files>
  <action>
    Remove old node_modules from root: `rm -rf node_modules`

    Run `pnpm install` to regenerate lockfile and install dependencies for both root and apps/web workspace. pnpm will create symlinks for @repo/typescript-config.

    Then verify the web app builds:
    ```bash
    pnpm --filter @repo/web build
    ```

    If build fails, check:
    - Path aliases resolve correctly (@/ → apps/web/src/)
    - TypeScript config extends work
    - All imports in src/ resolve properly

    Note: Builds may show warnings about unused dependencies - this is expected during migration. Critical: build must complete successfully with dist/ output.
  </action>
  <verify>
    <automated>pnpm --filter @repo/web build 2>&1 | grep -q "built in" || (echo "Build failed" && exit 1)</automated>
  </verify>
  <done>pnpm install succeeds, web app builds successfully, dist/ directory created with bundled output</done>
</task>

<task type="auto">
  <name>Task 5: Verify dev server and tests</name>
  <files>apps/web/</files>
  <action>
    Start dev server to verify it runs on correct port:
    ```bash
    timeout 10 pnpm --filter @repo/web dev &
    sleep 5
    curl -f http://localhost:8080 > /dev/null 2>&1
    ```

    If server doesn't start or port is wrong, check vite.config.ts port configuration.

    Then run existing tests to verify nothing broke:
    ```bash
    pnpm --filter @repo/web test
    ```

    All existing tests in src/test/ should pass without modification. If tests fail, check:
    - Test setup file (src/test/setup.ts) still works
    - Path aliases in test imports resolve correctly
    - vitest.config.ts paths are relative to apps/web/
  </action>
  <verify>
    <automated>pnpm --filter @repo/web test 2>&1</automated>
  </verify>
  <done>Dev server starts on localhost:8080, all existing tests pass, web app fully functional in new location</done>
</task>

</tasks>

<verification>
Run the following checks to confirm web app migration is complete:

1. Web app builds successfully:
   ```bash
   pnpm --filter @repo/web build
   ```

2. Dev server runs on correct port:
   ```bash
   pnpm --filter @repo/web dev
   # Visit http://localhost:8080 in browser
   ```

3. All tests pass:
   ```bash
   pnpm --filter @repo/web test
   ```

4. TypeScript compilation works:
   ```bash
   pnpm --filter @repo/web exec tsc --noEmit
   ```

5. Turbo can run web app tasks:
   ```bash
   turbo build --filter=@repo/web
   turbo test --filter=@repo/web
   ```
</verification>

<success_criteria>
- [ ] All source files moved to apps/web/ with git history
- [ ] apps/web/package.json exists with correct name and dependencies
- [ ] Configuration files updated with correct paths
- [ ] TypeScript config extends @repo/typescript-config/react.json
- [ ] Path alias @/ resolves to apps/web/src/
- [ ] pnpm install completes without errors
- [ ] Web app builds successfully (pnpm --filter @repo/web build)
- [ ] Dev server runs on localhost:8080
- [ ] All existing tests pass
- [ ] Turbo can orchestrate web app tasks
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md` documenting:
- Files moved and their new locations
- Configuration changes made (tsconfig, vite.config, package.json)
- Build output verification
- Test results
- Any path or import issues encountered and resolved
- Confirmation that web app works identically to before migration
</output>
