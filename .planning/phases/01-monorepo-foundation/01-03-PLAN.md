---
phase: 01-monorepo-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages/meditation-core/package.json
  - packages/meditation-core/tsconfig.json
  - packages/meditation-core/src/index.ts
  - packages/meditation-core/src/index.test.ts
autonomous: true
requirements: [MONO-04]

must_haves:
  truths:
    - "meditation-core package exists with TypeScript exports"
    - "Package can be imported by other workspace packages"
    - "TypeScript types resolve correctly"
  artifacts:
    - path: "packages/meditation-core/package.json"
      provides: "Package configuration with exports"
      contains: "exports"
    - path: "packages/meditation-core/src/index.ts"
      provides: "Package entry point"
      min_lines: 3
    - path: "packages/meditation-core/src/index.test.ts"
      provides: "Package test scaffold"
      min_lines: 5
  key_links:
    - from: "packages/meditation-core/package.json"
      to: "packages/meditation-core/src/index.ts"
      via: "exports field"
      pattern: "\"\\.\": \"\\./src/index\\.ts\""
    - from: "packages/meditation-core/tsconfig.json"
      to: "@repo/typescript-config/base.json"
      via: "extends field"
      pattern: "\"extends\": \"@repo/typescript-config/base\\.json\""
---

<objective>
Create meditation-core shared package with placeholder exports and test infrastructure.

Purpose: Establish the foundational shared package that will hold meditation business logic in Phase 2. This plan creates the package scaffold with TypeScript configuration, exports field, and basic test setup, but no actual meditation logic yet (that comes in Phase 2 extraction).

Output: Empty but functional @repo/meditation-core package that other workspace packages can import, with TypeScript types resolving correctly and a passing test to verify package setup.
</objective>

<execution_context>
@/Users/hyojin.yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojin.yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package structure and configuration</name>
  <files>packages/meditation-core/package.json, packages/meditation-core/tsconfig.json</files>
  <action>
    Create packages/meditation-core/ directory with two configuration files:

    1. package.json:
    ```json
    {
      "name": "@repo/meditation-core",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "exports": {
        ".": "./src/index.ts"
      },
      "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "lint": "echo 'No linter configured yet'",
        "test": "vitest run"
      },
      "devDependencies": {
        "@repo/typescript-config": "workspace:*",
        "typescript": "^5.8.3",
        "vitest": "^3.2.4"
      }
    }
    ```

    Key points:
    - exports field defines entry point as src/index.ts (JIT package, no build step needed in Phase 1)
    - type: module for ESM support
    - workspace:* protocol for internal dependencies per Turborepo best practice
    - vitest for testing (will inherit config from root)

    2. tsconfig.json:
    ```json
    {
      "extends": "@repo/typescript-config/base.json",
      "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src"
      },
      "include": ["src"]
    }
    ```

    This extends the shared base config created in plan 01-01, adding only package-specific paths.
  </action>
  <verify>
    <automated>test -f packages/meditation-core/package.json && test -f packages/meditation-core/tsconfig.json && grep -q '"@repo/meditation-core"' packages/meditation-core/package.json</automated>
  </verify>
  <done>Package configuration files created with correct exports field and TypeScript config extending shared base</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder source and test files</name>
  <files>packages/meditation-core/src/index.ts, packages/meditation-core/src/index.test.ts</files>
  <action>
    Create packages/meditation-core/src/ directory with two files:

    1. src/index.ts (placeholder export):
    ```typescript
    /**
     * @repo/meditation-core
     *
     * Shared meditation business logic for SimpleMeditation apps.
     *
     * Phase 1: Package scaffold only (placeholder exports)
     * Phase 2: Extract meditation store, types, utilities from web app
     */

    export const PACKAGE_NAME = '@repo/meditation-core';
    export const PACKAGE_VERSION = '0.0.0';

    // Placeholder function to verify package imports work
    export function validatePackage(): boolean {
      return true;
    }
    ```

    2. src/index.test.ts (basic test to verify package setup):
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { PACKAGE_NAME, validatePackage } from './index';

    describe('@repo/meditation-core', () => {
      it('exports package metadata', () => {
        expect(PACKAGE_NAME).toBe('@repo/meditation-core');
      });

      it('validatePackage returns true', () => {
        expect(validatePackage()).toBe(true);
      });
    });
    ```

    This test verifies that the package can be imported and exports work correctly. It will be expanded with real tests in Phase 2 when actual meditation logic is extracted.
  </action>
  <verify>
    <automated>test -f packages/meditation-core/src/index.ts && test -f packages/meditation-core/src/index.test.ts && grep -q "validatePackage" packages/meditation-core/src/index.ts</automated>
  </verify>
  <done>Source files created with placeholder exports and basic test coverage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install dependencies and verify package works</name>
  <files>pnpm-lock.yaml</files>
  <behavior>
    - Test 1: pnpm install links @repo/meditation-core as workspace package
    - Test 2: Package test runs and passes
    - Test 3: TypeScript compilation succeeds without errors
  </behavior>
  <action>
    Run `pnpm install` to ensure meditation-core package is discovered and linked by pnpm workspaces.

    Then run the package test to verify setup:
    ```bash
    pnpm --filter @repo/meditation-core test
    ```

    Verify TypeScript compilation works:
    ```bash
    pnpm --filter @repo/meditation-core exec tsc --noEmit
    ```

    If tests fail, check:
    - vitest is installed as devDependency
    - src/index.test.ts imports resolve correctly
    - tsconfig.json extends @repo/typescript-config/base.json successfully

    Note: The build script uses tsc but we're using JIT approach (no build needed) - it's defined for future use in Phase 2.
  </action>
  <verify>
    <automated>pnpm --filter @repo/meditation-core test</automated>
  </verify>
  <done>pnpm discovers package, tests pass, TypeScript compilation succeeds, package ready for import by other workspace packages</done>
</task>

<task type="auto">
  <name>Task 4: Verify package can be imported</name>
  <files>packages/meditation-core/</files>
  <action>
    Create a temporary test file in apps/web to verify the meditation-core package can be imported:

    ```bash
    cat > /tmp/test-import.ts << 'EOF'
    import { validatePackage, PACKAGE_NAME } from '@repo/meditation-core';
    console.log('Package import successful:', PACKAGE_NAME);
    console.log('Validation:', validatePackage());
    EOF
    ```

    Then try to run it with Node (using tsx or ts-node if available):
    ```bash
    cd apps/web && npx tsx /tmp/test-import.ts
    ```

    If import fails, check:
    - pnpm linked the package correctly (check node_modules/@repo/meditation-core is a symlink)
    - exports field in meditation-core/package.json is correct
    - TypeScript config in web app includes workspace packages in resolution

    After verification, delete the temporary test file. This confirms the package wiring works before Phase 2 extraction begins.
  </action>
  <verify>
    <automated>cd packages/meditation-core && pnpm exec tsc --noEmit && pnpm list @repo/typescript-config 2>/dev/null | grep -q typescript-config</automated>
  </verify>
  <done>Package imports work from other workspace packages, TypeScript resolution functional, ready for Phase 2 code extraction</done>
</task>

</tasks>

<verification>
Run the following checks to confirm meditation-core package is ready:

1. Package tests pass:
   ```bash
   pnpm --filter @repo/meditation-core test
   ```

2. TypeScript compilation succeeds:
   ```bash
   pnpm --filter @repo/meditation-core exec tsc --noEmit
   ```

3. Package is discovered by pnpm:
   ```bash
   pnpm list --recursive --depth=0 | grep meditation-core
   ```

4. Turbo can run package tasks:
   ```bash
   turbo test --filter=@repo/meditation-core
   ```

5. Package exports are accessible:
   ```bash
   node -e "import('@repo/meditation-core').then(m => console.log(m.PACKAGE_NAME))"
   ```
</verification>

<success_criteria>
- [ ] packages/meditation-core/ directory created with correct structure
- [ ] package.json has exports field pointing to src/index.ts
- [ ] tsconfig.json extends @repo/typescript-config/base.json
- [ ] src/index.ts exports placeholder functions
- [ ] src/index.test.ts has passing test
- [ ] pnpm install links package correctly
- [ ] Package tests pass (pnpm --filter @repo/meditation-core test)
- [ ] TypeScript compilation succeeds
- [ ] Package can be imported by other workspace packages
- [ ] Turbo can orchestrate package tasks
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-03-SUMMARY.md` documenting:
- Package structure created
- exports field configuration
- Test results showing package setup works
- Verification that other packages can import from @repo/meditation-core
- Any TypeScript resolution or import issues encountered
- Confirmation that package is ready for Phase 2 logic extraction
</output>
