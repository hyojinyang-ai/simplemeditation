---
phase: 03-mobile-app-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/tsconfig.json
  - apps/mobile/metro.config.js
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/tracker.tsx
  - apps/mobile/app/(tabs)/analytics.tsx
  - apps/mobile/app/(tabs)/settings.tsx
  - pnpm-workspace.yaml
autonomous: true
requirements: [APP-01, APP-02, APP-03, APP-04]

must_haves:
  truths:
    - Expo app created in apps/mobile/ directory
    - Four tab screens render without crashes
    - React Native Paper components display correctly
    - App builds successfully for iOS Simulator
  artifacts:
    - path: apps/mobile/package.json
      provides: Expo app dependencies and workspace configuration
      contains: "@expo/router"
    - path: apps/mobile/app/(tabs)/_layout.tsx
      provides: Tab navigation with 4 tabs
      exports: ["default"]
    - path: apps/mobile/app/_layout.tsx
      provides: PaperProvider setup
      min_lines: 20
  key_links:
    - from: apps/mobile/app/_layout.tsx
      to: react-native-paper
      via: PaperProvider import
      pattern: "PaperProvider"
    - from: apps/mobile/app/(tabs)/_layout.tsx
      to: expo-router
      via: Tabs component
      pattern: "<Tabs"
---

<objective>
Create Expo app scaffold in monorepo with file-based tab navigation and Material Design UI foundation.

Purpose: Establish iOS app structure enabling subsequent phases to add business logic
Output: Running iOS app with 4 placeholder tab screens using React Native Paper components
</objective>

<execution_context>
@/Users/hyojin.yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojin.yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-app-scaffold/03-RESEARCH.md

# Existing monorepo structure from Phase 1
@package.json
@pnpm-workspace.yaml

# Existing shared packages (Phase 2)
@packages/meditation-core/src/index.ts
@packages/meditation-core/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Expo app with tabs template</name>
  <files>
    apps/mobile/package.json
    apps/mobile/app.json
    apps/mobile/tsconfig.json
    apps/mobile/metro.config.js
    apps/mobile/app/_layout.tsx
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/app/(tabs)/tracker.tsx
    apps/mobile/app/(tabs)/analytics.tsx
    apps/mobile/app/(tabs)/settings.tsx
  </files>
  <action>
Run `cd apps && npx create-expo-app@latest mobile --template tabs` to create Expo Router app.

After creation:
- Update package.json to use workspace protocol for meditation-core: `"@repo/meditation-core": "workspace:*"`
- Verify pnpm-workspace.yaml includes apps/mobile in workspaces array
- Run `pnpm install` from monorepo root to link workspace packages

Template creates:
- app/(tabs)/_layout.tsx with 2 default tabs (we'll modify to 4 in next task)
- app/_layout.tsx root layout (we'll add PaperProvider in Task 2)
- Expo SDK 55 dependencies
- Metro config with monorepo auto-detection

Do NOT run prebuild or build commands yet - keep it as Expo managed app until Task 4.
  </action>
  <verify>
    <automated>ls apps/mobile/app.json && cat apps/mobile/package.json | grep '@repo/meditation-core'</automated>
  </verify>
  <done>
apps/mobile/ directory exists with Expo Router structure
package.json includes workspace dependency for meditation-core
pnpm install completes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure 4-tab navigation with SimpleMeditation theme</name>
  <files>
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/app/(tabs)/tracker.tsx
    apps/mobile/app/(tabs)/analytics.tsx
    apps/mobile/app/(tabs)/settings.tsx
  </files>
  <action>
Modify app/(tabs)/_layout.tsx to define 4 tabs:
1. index (Meditate) - icon: "meditation" from MaterialCommunityIcons
2. tracker (Tracker) - icon: "heart-pulse"
3. analytics (Analytics) - icon: "chart-line"
4. settings (Settings) - icon: "cog"

Use zen-blue color (#6366F1) for tabBarActiveTintColor, gray (#9CA3AF) for inactive.
Set headerShown: false in screenOptions.

For each tab screen (index, tracker, analytics, settings):
- Create placeholder View with StyleSheet
- Add Text component showing tab name (variant="headlineLarge")
- Add Text showing "Coming soon" message
- Use flex: 1, centered layout with padding: 20

Follow Pattern 1 from RESEARCH.md (Tab Navigation) but with 4 tabs instead of example structure.
  </action>
  <verify>
    <automated>grep -c "Tabs.Screen" apps/mobile/app/(tabs)/_layout.tsx</automated>
  </verify>
  <done>
4 tab screen components in _layout.tsx
All 4 tab files (index, tracker, analytics, settings) exist with placeholder content
Tab navigation uses SimpleMeditation brand colors
  </done>
</task>

<task type="auto">
  <name>Task 3: Install React Native Paper and configure theme provider</name>
  <files>
    apps/mobile/package.json
    apps/mobile/app/_layout.tsx
  </files>
  <action>
Install React Native Paper and dependencies:
```bash
cd apps/mobile && npx expo install react-native-paper react-native-safe-area-context
```

Modify app/_layout.tsx to wrap with providers:
1. Import PaperProvider, MD3LightTheme from react-native-paper
2. Import SafeAreaProvider from react-native-safe-area-context
3. Create theme object extending MD3LightTheme with SimpleMeditation colors:
   - primary: #6366F1 (zen-blue)
   - secondary: #E0E7FF (zen-blue-light)
   - tertiary: #C7D2FE (zen-lavender)
   - background: #FFFFFF
   - surface: #F9FAFB
4. Wrap Stack with SafeAreaProvider > PaperProvider (theme prop)

Follow Pattern 3 from RESEARCH.md (React Native Paper Provider Setup).

Import Stack from expo-router, not NavigationContainer (Expo Router handles navigation state).
  </action>
  <verify>
    <automated>grep "PaperProvider" apps/mobile/app/_layout.tsx && grep "SafeAreaProvider" apps/mobile/app/_layout.tsx</automated>
  </verify>
  <done>
react-native-paper and react-native-safe-area-context installed
Root layout wraps with PaperProvider using SimpleMeditation theme
SafeAreaProvider configured for notch/status bar handling
  </done>
</task>

<task type="auto">
  <name>Task 4: Update placeholder screens with Paper components</name>
  <files>
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/app/(tabs)/tracker.tsx
    apps/mobile/app/(tabs)/analytics.tsx
    apps/mobile/app/(tabs)/settings.tsx
  </files>
  <action>
Replace plain Text components in all 4 tab screens with React Native Paper components:

For index.tsx (Meditate screen):
- Import Text, Button from react-native-paper
- Use Text variant="headlineLarge" for "SimpleMeditation"
- Use Text variant="bodyMedium" for "Meditation player coming in Phase 5"
- Add Button mode="contained" with onPress={() => console.log('Start meditation')}

For tracker.tsx:
- Import Text, Card from react-native-paper
- Use Card with Card.Title and Card.Content showing "Mood Tracker"
- Text variant="bodyMedium" for "History coming in Phase 5"

For analytics.tsx:
- Import Text, Card from react-native-paper
- Use Card with Card.Title showing "Analytics"
- Text variant="bodyMedium" for "Charts coming in Phase 5"

For settings.tsx:
- Import Text, List from react-native-paper
- Use List.Item with title="Settings" and description="Configuration coming in Phase 6"

This validates Paper components render correctly before adding business logic in future phases.
  </action>
  <verify>
    <automated>grep "from 'react-native-paper'" apps/mobile/app/(tabs)/index.tsx</automated>
  </verify>
  <done>
All 4 tab screens import and use React Native Paper components
Paper components render with Material Design 3 theming
Console logs verify button interactions work
  </done>
</task>

</tasks>

<verification>
Run development build and verify on iOS Simulator:
```bash
cd apps/mobile
npx expo start
```

Open in iOS Simulator (press 'i' in terminal). Verify:
- App launches without crashes
- Bottom tab navigation shows 4 tabs with correct icons
- Tapping each tab switches screens
- Paper components render with correct theme colors (zen-blue primary)
- Text uses Material Design typography
- Safe area insets respected (no overlap with notch)

If expo-router warnings appear about native modules, run `npx expo prebuild` to generate native projects (this creates ios/ and android/ folders but keeps managed workflow).
</verification>

<success_criteria>
- apps/mobile/ directory exists with Expo Router structure
- package.json includes workspace dependency for @repo/meditation-core
- Tab navigation renders 4 tabs (index, tracker, analytics, settings)
- React Native Paper components display with SimpleMeditation theme
- App launches on iOS Simulator without crashes
- All tabs navigable and render placeholder content
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-app-scaffold/03-01-SUMMARY.md` using summary template.
</output>
