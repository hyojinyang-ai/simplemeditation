{
  "name": "simple-meditation-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "turbo dev --filter=@repo/web",
    "dev:ios": "turbo dev --filter=@repo/ios",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "ios": "pnpm --filter @repo/ios ios"
  },
  "devDependencies": {
    "turbo": "^2.8.0"
  }
}
